
# Initializing the git
git init

# Creating the develop branch
git branch -m develop

# Added the remote
git remote add origin https://github.com/rishabh2312/least-square-function-selection.git

# Adding a file for the Initial Commit
git add QUICKSTART.md

# Committing the file
git commit -m "Initial Commit"

# Pushed the changes to the develop branch
git push -u origin develop

# Creating the feature branch feature/python-task-assignment
git branch -b feature/python-task-assignment

# Check out the feature branch
git checkout -b feature/python-task-assignment

# Adding all the files to commit
git add .

# Committing the changes
git commit -m "Implementation for the assignment of least square function selection"

# Pushing the changes to the feature branch
git push --set-upstream origin feature/python-task-assignment

